export const userSliceSelector = (state) => state.userSlice;
export const productSliceSelector = (state) => state.productSlice;
export const cartSliceSelector = (state) => state.cartSlice;
export const customerSliceSelector = (state) => state.customerSlice;
export const billSliceSelector = (state) => state.billSlice;
export const billDetailSliceSelector = (state) => state.billDetailSlice;
